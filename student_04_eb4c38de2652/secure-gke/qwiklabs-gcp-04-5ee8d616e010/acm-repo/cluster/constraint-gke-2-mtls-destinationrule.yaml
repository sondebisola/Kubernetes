apiVersion: constraints.gatekeeper.sh/v1beta1
kind: DestinationRuleTLSEnabledBeta
metadata:
  name: destinationrule-mtls-enabled
  annotations:
    configsync.gke.io/cluster-name-selector: CLUSTER_2
spec:
  enforcementAction: deny
  match:
    kinds:
      - apiGroups:
          - networking.istio.io
        kinds:
          - DestinationRule
